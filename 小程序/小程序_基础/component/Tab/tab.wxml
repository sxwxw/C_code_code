<view class="tabs" hover-class="none" hover-stop-propagation="false">
    <view class="tabs_title">
         <view  
         wx:for="{{tabs}}" wx:key="id" class="title_item {{item.isActive?'active':''}}" bindtap="listTap" data-index="{{index}}">
           {{item.name}}
         </view>   
    </view>
    <view class="tabs_content">
      <!-- slot标签就是占位符 -->
      <slot></slot>
    </view>
</view>
<view>{{aaaa}}</view>